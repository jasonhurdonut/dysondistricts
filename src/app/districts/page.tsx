import { redirect } from "next/navigation";

// The "Meet the Districts" page is disabled for now. To re-enable it, restore
// the previous version of this file from git history and delete this stub.
export default function DistrictsPage() {
  redirect("/");
}
